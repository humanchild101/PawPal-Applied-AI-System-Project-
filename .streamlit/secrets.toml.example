# Copy this file to secrets.toml (same folder) and paste in your real key.
# secrets.toml is gitignored, so your real key never gets committed.
# Get a free key at https://console.groq.com/keys
GROQ_API_KEY = "paste-your-groq-api-key-here"
